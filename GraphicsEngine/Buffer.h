#pragma once
#include "GL/glew.h"
#include <vector>

class Buffer
{
protected:
	

public:
	Buffer(GLuint size);

	/*Buffer(const GLfloat* data, const GLfloat* colorData);
	virtual void bind() = 0;*/
};

