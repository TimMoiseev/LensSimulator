<UserControl x:Class="LensSimulator.View._2DPlot.Plot2D"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:local="clr-namespace:LensSimulator.View._2DPlot" 
             xmlns:conv="clr-namespace:LensSimulator.Converters"
             mc:Ignorable="d"
             Name="Plot2DControll"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <conv:HalfDoubleValueConverter x:Key="HalfValueConverter"/>
    </UserControl.Resources>
    <ItemsControl ItemsSource="{Binding ElementName=Plot2DControll, Path=Lenses}">
        <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate>
                <Canvas x:Name="Plot2DMainCanvas" Background="Black" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Visibility="Visible">
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="MouseMove">
                            <i:InvokeCommandAction Command="{Binding ElementName=Plot2DControll, Path=Plot2DMouseMoveCommand}"
                           PassEventArgsToCommand="True">
                            </i:InvokeCommandAction>
                        </i:EventTrigger>
                        <i:EventTrigger EventName="MouseDown">
                            <i:InvokeCommandAction Command="{Binding ElementName=Plot2DControll, Path=Plot2DMouseDownCommand}"
                                PassEventArgsToCommand="True">
                            </i:InvokeCommandAction>
                        </i:EventTrigger>
                        <i:EventTrigger EventName="MouseUp">
                            <i:InvokeCommandAction Command="{Binding ElementName=Plot2DControll, Path=Plot2DMouseUpCommand}"
                                PassEventArgsToCommand="True">
                            </i:InvokeCommandAction>
                        </i:EventTrigger>
                    </i:Interaction.Triggers>

                    <!--<local:AxisLine2D x:Name="Plot2DMainAxisLine" 
                        Width="{Binding ElementName=Plot2DMainCanvas, Path=ActualWidth}" 
                        Height="{Binding ElementName=Plot2DMainCanvas, Path=ActualHeight}" HorizontalAlignment="Center" VerticalAlignment="Center"></local:AxisLine2D>-->
                </Canvas>
            </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
    </ItemsControl>
    
</UserControl>
